<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" 
    android:saveEnabled="false" >

    <LinearLayout
        style="?attr/Layout"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="5dp"
        android:layout_marginRight="5dp"
        android:orientation="horizontal" 
        android:saveEnabled="false"
        >

        <EditText
            android:id="@+id/preset_search_edit"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:minHeight="34dp" 
            android:layout_marginBottom="1dp"
            android:layout_weight="1"
            android:drawableEnd="@drawable/baseline_clear_white_36dp"
            android:drawableRight="@drawable/baseline_clear_white_36dp"
            android:drawableTint="?attr/colorAccent"
            android:hint="@string/preset_search_hint"
            android:imeOptions="actionSearch"
            android:inputType="text"
            android:maxLines="1" 
            android:saveEnabled="false" />

        <androidx.appcompat.widget.ActionMenuView
            android:id="@+id/preset_menu"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_gravity="end"
            android:layout_weight="2"
            android:layout_marginBottom="1dp"         
            android:minHeight="?attr/actionBarSize" 
            android:background="?attr/colorPrimary"
            android:saveEnabled="false"
            android:visibility="gone" />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/preset_results"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical" 
        android:saveEnabled="false"/>

    <LinearLayout
        android:id="@+id/preset_presets"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:orientation="vertical" 
        android:saveEnabled="false"/>

</LinearLayout>